# 法衡 AI · 企业多智能体工作台（Demo）· 项目总结

> 版本：v0.2.0 ｜ 完成时间：2026-09 ｜ 状态：端到端验证通过，可演示；已容器化（Docker 单镜像交付）
>
> 相关文档：[README.md](README.md)（启动指南）· [技术方案](AI合同智能处理系统_技术方案.md)（架构设计与演进规划）

---

## 一、项目概述

**法衡 AI（FaHeng AI）** 是一个面向客户演示的企业多智能体工作台，当前交付合同双智能体，覆盖商务合同两大核心场景：

- **合同起草**：输入关键词 → AI 生成结构完整的合同模板 → 在线编辑/对话式修订 → 导出 Word
- **合同审查**：上传 PDF/Word 合同 → AI 按企业规则库逐条审查 → 风险清单（三栏工作台）→ 逐项采纳/修改/不采纳 → 导出修改后合同与审核报告

**产品原则**：AI 负责识别与生成，人负责确认与决策。所有 AI 输出均为"草稿/建议"，每条风险可追溯至具体规则号，决策记录完整保留。

**Demo 定位**：最小可用闭环。不做用户系统、不做 Celery/Redis、不做 PostgreSQL——内存 session + 文件目录，重启即失，单次会话内完成演示。

## 二、需求与范围决策

| 需求 | 范围决策 |
|---|---|
| 合同起草（关键词→模板→编辑→导出） | ✅ 全部实现 |
| 合同审查（上传→风险清单→决策→导出） | ✅ 全部实现，流程交互对齐两个 HTML 原型 |
| 履约提醒 / 会议纪要 / 员工管理 | ⏸ 后续规划（见第八节） |
| 用户登录（演示账号） | ✅ 单账号 + 本地 token 白名单，未登录访问业务页跳 `/login` |
| RAG / Dify / 向量库 | ❌ Demo 不用——26 条规则全量注入 prompt（约 1.5K 字），无漏召回、结果可复现 |
| 数据库 / 鉴权 / 定时任务 | ❌ Demo 不做（演示鉴权用 `.env` 配置 + 后端内存 token） |

## 三、系统架构

```
faheng-web (Vue3 + Element Plus + Pinia)     :58068
    │  /api 代理
faheng-api (FastAPI + asyncio)               :58069
    │  OpenAI 兼容接口
LLM 网关 (aihub.ssturing.com / deepseek-v4-flash)
```

| 层 | 技术选型 | 说明 |
|---|---|---|
| 前端 | Vue 3 + Vite + Element Plus + Pinia + markdown-it | 三页面路由，审查工作台用 Pinia 状态机 |
| 后端 | Python 3.14 + FastAPI + pydantic 2 | 异步 IO，pydantic 校验 LLM 结构化输出 |
| LLM | OpenAI 兼容网关（deepseek-v4-flash） | 模型带思维链，max_tokens 需给足 |
| 文档解析 | python-docx / pdfplumber | DOCX 保段落结构，PDF 提取文本行 |
| 文档导出 | python-docx + BeautifulSoup | 就地替换 / 重建 / HTML→docx 保真 / markdown 转换 共四条路径 |
| 存储 | 全程内存 BytesIO，零落盘 | 上传走 `UploadFile.read()`，导出走 `StreamingResponse(BytesIO)`；测试合同构建期嵌入镜像，运行时只读 |
| 交付 | Docker 多阶段构建单镜像 | 前端 build 产物 + 后端由同一 uvicorn 进程 serve（:58069，同源无 CORS 问题） |

## 四、功能流程

### 4.1 合同审查（核心流程，对应 ERP 审核原型）

```
上传 DOCX/PDF
  → 条款切分（按"第X条"，记录段落区间锚点）
  → 开始 AI 审查（后台任务，分批调 LLM，前端轮询进度）
     每批 ≤8 条条款 / ≤2500 字符，规则库 26 条全量注入 prompt
  → 风险清单（条款号/等级/命中规则/风险说明/影响/建议替换条款）
  → 三栏工作台：
     [风险列表(等级×状态双过滤)] [合同全文+条款高亮联动] [法务意见面板]
  → 逐项决策：采纳 / 修改(编辑后采纳) / 不采纳(选原因)，支持撤销
  → 导出修改后合同（被替换条款黄色高亮）+ 审核报告
```

### 4.2 合同起草

```
表单输入（关键词 + 可选类型/甲乙方/补充要求）
  → LLM 生成 Markdown 合同（"第X条"编号、占位符【金额】【日期】、甲方立场）
  → TipTap WYSIWYG 单栏编辑器（A4 纸预览 + 工具栏，防抖保存）
  → 对话式修订：每次"当前全文 + 指令"整篇重生成（避免多轮上下文污染）
  → 导出 Word：前端把当前 TipTap HTML 传给后端 → HTML→docx 保真转换
    （h1/h2/h3 映射标题与字号、p 缩进首行 2 字符、ul/ol 列表、table 边框、
     strong/em/u 映射 run 的 bold/italic/underline；甲方/乙方/鉴于行的多行 <p>
     拆分正确避免 IndexError）
```

### 4.3 API 清单

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/api/health` | 探活（含 LLM 网关连通检查） |
| POST | `/api/review/upload` | 上传合同，返回切分后的条款列表 |
| POST | `/api/review/sample` | 载入内置测试合同（19 条款埋坑版） |
| POST | `/api/review/{id}/start` | 启动后台 AI 审查 |
| GET | `/api/review/{id}/status` | 轮询进度（percent + stage 文案） |
| GET | `/api/review/{id}/result` | 获取风险清单 + 风险分 + 决策 |
| PUT | `/api/review/{id}/decisions` | 保存决策（全量幂等覆盖） |
| POST | `/api/review/{id}/export` | 导出修改后合同 docx |
| POST | `/api/review/{id}/report/export` | 导出审核报告 docx |
| POST | `/api/draft/generate` | 关键词生成合同模板 |
| GET/PUT | `/api/draft/{id}`(`/content`) | 读取/保存草稿 |
| POST | `/api/draft/{id}/revise` | 对话式修订 |
| POST | `/api/draft/{id}/export` | 导出 Word |
| POST | `/api/auth/login` | 登录校验，返回 `access_token` |
| POST | `/api/auth/logout` | 注销（使 token 失效） |
| GET | `/api/auth/me` | 当前登录用户（受 `require_auth` 保护） |

## 五、关键技术设计

### 5.1 规则全量注入（替代 RAG）

26 条审核规则（`rules/erp_rules.md`，含规则号/触发条件/建议条款/等级）全量拼进审查 prompt。理由：

- 规则总量 ≈ 1.5K 字，远小于模型上下文窗口
- 全量注入 = 每条规则每次都参与判断，**无漏召回、结果可复现**，且每条风险可追溯规则号
- 换客户规则 = 改一个 markdown 文件，零迁移成本

**RAG 升级触发线**（满足任一再升级，只换"规则注入方式"，prompt/前端/schema 不动）：规则 > 100 条或 30K token；多合同类型挂不同规则库；法务要求规则级版本管理与审计。

### 5.2 LLM 结构化输出防御（继承 Docspect 经验并强化）

`app/llm/client.py` 的 `chat_json()` 三段式处理：

1. **clean**：去 ```json 围栏 → 定位首个 `{`/`[` → `json.loads`
2. **fix**：失败则去尾逗号再试
3. **salvage**：截断时（`finish_reason=length`）取最后完整 `}` 补齐括号，逐元素 pydantic 校验丢弃残缺项
4. **retry**：失败重试 ≤2 次，把上次错误原因附进重试消息

代码侧后处理兜底：非法 `clause_id` 丢弃、`matched_rules` 过滤为已知规则号、按 `(clause_id, title)` 去重。

### 5.3 中文合同条款切分

`app/parser/clause_splitter.py`（纯函数，导出正确性的前提）：

- 正则 `^\s*第[一二三四五六七八九十百零〇\d]+条` 切条款；首个条款前 = preamble；`（盖章）/授权代表/签署日期` 起 = tail（不审查，导出原样保留）
- 每个条款记录 **docx 段落区间锚点**（`start_idx/end_idx`），导出回填依赖它
- 降级：无"第X条"命中时按空行分块为伪条款，流程不中断

### 5.4 审查后导出的三条路径（最大技术难点）

| 场景 | 方案 |
|---|---|
| DOCX 上传 | **原文档就地替换**：python-docx 重开原件，只动命中条款段落区间——首段保样式改文本+黄色高亮，区间其余段落删除，多余建议段落插入。表格/页眉/签署栏全保留 |
| PDF 上传 | 无法保真转 docx → **重建降级**：用切分结果重建干净 Word（显式设置 `w:eastAsia` 中文字体），前端明示"重新排版导出" |
| 起草导出（主路径） | **前端 HTML → docx**：把 TipTap 当前 HTML 一起 POST 给后端，BeautifulSoup 解析后用 `export_html_docx` 忠实地转 docx——`h1/h2/h3` 标题、`p` 首行缩进 2 字符、居中/左对齐、`ul/ol` 列表、`table` 边框、行内 `strong/em/u` 映射 run 样式，所见即所得 |
| 起草导出（降级） | 不传 HTML 时回退到 markdown → docx 轻量转换（`#`→Heading、`**粗体**`拆 run、列表原样文本） |

同一条款多个已采纳风险：按风险等级降序合并为多条替换段落，确定性规则、无需 LLM。

### 5.5 其他要点

- **风险分由代码确定性计算**：`min(100, high×15 + medium×7)`，不让 LLM 拍脑袋（吸取 Docspect 教训）
- **temperature**：审查 = 0（可复现），起草/修订 = 0.3
- **max_tokens**：审查 12K / 起草 16K——网关模型带思维链（reasoning_content 消耗 token），给小了会输出为空
- **emoji 剥离**：模型输出夹 emoji 会炸 Windows GBK 控制台并干扰 JSON 解析，统一过滤
- **决策乐观更新**：点采纳立即变色并自动跳下一条待处理（复刻原型交互），后端 PUT 幂等全量覆盖
- **两步式上传**（upload → start）：对齐原型交互，用户上传后先看到"已切分 N 条条款"再开始审查
- **演示鉴权（最小闭环）**：账号密码写 `faheng-api/.env`（`DEMO_USERNAME` / `DEMO_PASSWORD`），后端内存字典 `AUTH_TOKENS` 维护 token 白名单（`secrets.token_hex` + TTL），`require_auth` 依赖统一挂在业务路由上；前端 token 存 `localStorage`，axios 拦截器自动加 `Authorization: Bearer`，响应拦截器对 `/auth/login` 的 401 单独提示"用户名或密码错误"（不清态、不跳转），其他 401 才清态跳登录页；未登录访问 `/`、`/draft`、`/review` 由路由 `beforeEach` 重定向到 `/login`

### 5.6 起草导出的 HTML→docx 保真转换

为让"Word 导出 = A4 纸所见"，起草页把 Markdown 作为"保存/AI 修订的单一真相源"，但在导出这一刻把 TipTap 的 HTML 一起 POST 给后端：

- 前端 [RichEditor.vue](faheng-web/src/components/draft/RichEditor.vue) 通过 `defineExpose({ getHtml })` 把 `editor.getHTML()` 暴露给父组件；
- [DraftView.vue](faheng-web/src/views/DraftView.vue) `onExport` 取 HTML 后随 `{ html }` body 调导出接口；
- 后端新增 `ExportIn(html?: str)`，service 优先走 `export_html_docx`，未传 HTML 时回退原 markdown 路径；
- 后端 `export_html_docx` 用 BeautifulSoup 解析顶层块级节点，按前端 `.docx-prose` CSS 严格映射：h1=22pt 居中、h2=14pt 黑体、h3=12pt 黑体、p=12pt 仿宋首行缩进 2 字符、ul/ol=列表缩进 2em、table=A4 可用宽度固定、strong/em/u 映射 run 的 bold/italic/underline；
- 行内节点用 `deepcopy` 复制原节点链到新 `<p>`，避免 `<strong>` 等标签被字面化进 docx；
- 多行 `<p>`（含 `<br>`）按行切分后按 `_classify_line` 派发：`h2/h3` → 标题、`p`（甲方/乙方/鉴于行）→ 段落、纯文本 → 段落，兜底也按段落处理（关键 bug 修复）；
- 页边距与前端 A4 纸 CSS 的 `28mm 25mm` 一致（上下 2.8cm、左右 2.5cm）。

### 5.7 容器化交付（v0.2.0）

多阶段单镜像部署形态（根目录 [Dockerfile](Dockerfile) + [.dockerignore](.dockerignore)）：

- **阶段 1**：`node:20-alpine` 内 `npm ci && npm run build` 产出 `/web/dist`。`node_modules` 必须在 alpine 内重新生成——WSL/NTFS 跨边界会把 Windows 的 `node.exe` shim 带进镜像（`.dockerignore` 排除 `**/node_modules/`）；
- **阶段 2**：`python:3.11-slim` 安装依赖、拷入 `app/scripts/rules` 与前端产物，构建期执行 `make_sample_docx.py` 把测试合同嵌入镜像（运行时只读），非 root 用户 `faheng` 运行；
- **单进程同源**：`app/main.py` 在所有 `/api` 路由之后 `app.mount("/", StaticFiles(html=True))`——本地 dev 时 `web_dist/` 不存在，不挂载，双进程形态不受影响；
- **无状态化**（容器化的前提，本次改造的核心）：`config.py` 删除 `DATA_DIR/UPLOAD_DIR/EXPORT_DIR` 与 `ensure_dirs()`；上传合同字节驻留 `ReviewSession.upload_bytes`；六个导出函数全部改为返回 `io.BytesIO`；路由层 `FileResponse` → `StreamingResponse`——服务器磁盘全程不接触用户合同；
- **测试合同**：`make_sample_docx.py` 输出改到项目根，`/api/review/sample` 从 `BASE_DIR` 读取镜像内副本；
- **安全边界**：`.env` 被 `.dockerignore` 排除，密钥只在运行时 `--env-file` 注入；CORS 追加 `:58068` dev origin（同源部署下可关闭）；
- **依赖修正**：`httpx` 2.13.0 → 0.28.1（原版本号不存在）；
- **端口统一**：后端 8000 → 58069，前端 dev 5173 → 58068（vite proxy 同步改）；
- **健康探活**：HEALTHCHECK 复用 `/api/health`；`uvicorn --proxy-headers` 兼容前置反代；
- **离线交付**：`docker save` → `scp -P <端口>` → 目标服务器 `docker load`（tar 不含 `.env`，需在服务器另备）。

## 六、项目结构

```
ezContract/  # 仓库根目录（品牌：法衡 AI · 企业多智能体工作台）
├── README.md                       # 启动指南
├── Dockerfile                      # 多阶段镜像构建（单容器：前端产物 + 后端）
├── .dockerignore                   # 排除 .env / node_modules / 文档
├── 项目总结.md                      # 本文档
├── myContract/                     # 需求与技术方案
│   ├── AI合同智能处理系统_技术方案.md   # 含九章 Demo 实施方案
│   ├── ERP合同AI审核原型Demo.html     # 流程基准原型
│   └── 审核规则导入原型Demo.html
├── faheng-api/                 # FastAPI 后端 :58069
│   ├── rules/erp_rules.md          # 26 条规则库（换规则改这里）
│   ├── scripts/make_sample_docx.py # 生成埋坑测试合同（输出到项目根，构建期嵌入镜像）
│   ├── sample_contract.docx        # 内置测试合同（Docker 镜像内只读随镜像发布）
│   └── app/
│       ├── main.py / config.py / storage.py
│       ├── schemas/                # pydantic 模型（API 层 + LLM 输出层）
│       │   ├── auth.py              # 登录/登出 schema
│       │   └── ...
│       ├── llm/                    # client.py(三段式解析) prompts.py
│       ├── parser/                 # docx/pdf 解析 + 条款切分
│       ├── services/               # 审查流水线/起草/规则加载/docx 导出
│       └── routers/                # auth.py / review.py / draft.py
├── faheng-web/                 # Vue3 前端 :58068
│   └── src/
│       ├── views/                  # Home / Draft / Review / Login
│       ├── stores/                 # auth.js / review.js(五视图状态机) / draft.js
│       ├── router/                 # 路由 + beforeEach 守卫
│       └── components/
│           ├── draft/              # 表单 / RichEditor(TipTap WYSIWYG) / 修订面板
│           └── review/             # 上传/进度/统计/风险列表/合同正文/意见/报告
└── Docspect/                       # 参考项目（LLM 工程经验来源，非本项目代码）
```

## 七、测试验证

### 7.1 API 级 E2E（curl，走 Vite 代理全链路）

| 验证项 | 结果 |
|---|---|
| LLM 网关连通（health 含探活） | ✅ `llm: ok` |
| 规则库加载 | ✅ 26 条 |
| 测试合同切分 | ✅ 19 条"第X条"精确切分，含段落锚点 |
| AI 审查（3 批次） | ✅ 33 秒完成，21 项风险（16 高/5 中），规则号全部合法 |
| 决策 + 导出 | ✅ 采纳条款在导出 docx 中已替换+黄色高亮，签署栏保留，同条款区间替换正确 |
| 审核报告导出 | ✅ 决策表正确反映处理状态 |
| 起草生成 | ✅ 4.4K 字合同，"第X条"结构完整，占位符规范 |
| 起草修订 | ✅ "改分期付款"指令生效（40%签订/40%初验/20%终验） |
| 起草导出 | ✅ Word 标题/粗体/编号正常 |

### 7.2 浏览器 E2E（agent-browser 自动化走查）

| 流程 | 结果 |
|---|---|
| 审查：载入合同→AI 审查→三栏工作台 | ✅ 点风险项→条款红/黄底高亮+滚动联动；右栏意见完整 |
| 审查：采纳 / 修改保存 / 不采纳+原因 / 撤销 | ✅ 计数联动、自动跳下一条、已处理 tab 正确 |
| 审查：报告页 + 两个导出按钮 | ✅ 统计卡、明细表、下载触发、成功 toast |
| 起草：生成→手动编辑（防抖"已保存"）→修订→导出 | ✅ 全部通过（TipTap WYSIWYG 单栏，A4 纸所见即所得，导出 Word 版式与页面一致） |

### 7.3 开发中发现并修复的问题

1. **网关模型思维链吃 token**：初始 max_tokens 过小导致输出为空 → 调大至审查 12K/起草 16K
2. **GBK 控制台炸 emoji**：模型输出夹 emoji → LLM 客户端统一剥离
3. **OpinionPane 响应性 bug**：`const risk = store.currentRisk` 是快照，切换风险右栏不刷新 → 改 `computed()`
4. **ESLint 错误**：App.vue template 多根节点、ContractPane v-for key 位置 → 修复
5. **早期 chunker 中文缺陷**（Docspect 遗留问题在本项目重写规避）：按 `.?!` 切句对中文失效 → 自研"第X条"切分
6. **docx_export 多行 `<p>` 派发对 `kind="p"` 的 IndexError**：`_classify_line` 在甲方/乙方/鉴于行会返回 `"p"`，原派发直接 `int(tag_name[1])` 触发 IndexError，导致 `POST /api/draft/{id}/export` 返回 500 → 显式分流 `p_inlines/p_text` 走段落、`h2/h3` 走标题、兜底按段落处理
7. **起草导出 Word 中残留 `<strong>` 字面 HTML 标签**：原实现把 inline 节点 `str()` 序列化后再插入，HTML 标签字面出现在 docx → 改为 `deepcopy` 原始 inline 节点链（`<strong>/<em>/<u>`），标签真正映射成 run 的 bold/italic/underline
8. **WSL/NTFS 边界把 Windows node shim 带进 alpine 镜像**：`npm ci` 在 Windows 侧预装后 COPY 进 alpine 会带上 `node.exe` shebang → `.dockerignore` 排除 `**/node_modules/`，坚持在 alpine 内重新 `npm ci`
9. **requirements 里 `httpx==2.13.0` 不存在**：镜像构建 `pip install` 失败 → 修正为 0.28.1

## 八、已知限制与后续规划

### 已知限制（Demo 定位所致）

- 后端重启丢失会话数据（内存存储）；单进程，并发审查共享内存
- 后端重启后 token 白名单清空，已登录用户需重新登录；不持久化、不做 JWT 签名
- PDF 上传导出为重新排版的 Word（无法保留原版式，页面有提示）
- 无用户体系/权限；规则库为单文件、无版本管理
- 部署提示：镜像内不含 `.env`，目标服务器需自备并通过 `--env-file` 注入；公网暴露建议前置 Nginx/Cloudflare Tunnel（同源反代时后端 CORS 可关闭）

### 后续规划

| 方向 | 说明 | 是否需要 RAG |
|---|---|---|
| 履约提醒 | LLM 从已签合同提取 JSON 节点（付款日/交付日）→ 存表 → 定时扫描 → 推送 | 否 |
| 会议纪要 | ASR 转文字 → LLM 结构化 | 否 |
| 员工/工作内容管理 | CRUD + LLM 辅助字段 | 否 |
| 规则库扩容 | 超过升级触发线（>100 条/多类型/审计需求）时引入检索 | 视规模 |

架构升级路径已在[技术方案第九章](AI合同智能处理系统_技术方案.md)固化：从 Dify/RAG/PG/Celery 的完整架构看，Demo 实现的是其"简化分身"，升级时仅替换"规则注入方式"与"存储层"，prompt、schema、前端交互均无需重写。

## 九、演示脚本建议

1. **合同审查**（约 3 分钟）：首页 → 合同审查 → 载入测试合同 → 开始 AI 审查（进度动画约 1-2 分钟）→ 点几个风险项看三栏联动 → 演示采纳/修改/不采纳 → 查看审核报告 → 导出修改后合同，Word 打开看黄色高亮的替换条款
2. **合同起草**（约 3 分钟）：合同起草 → 输入关键词（如"ERP软件采购 私有云部署 三年订阅"）→ 生成（约 1 分钟）→ TipTap WYSIWYG 单栏编辑器（A4 纸所见即所得，工具栏 B/I/H1/H2/P/列表/表格/对齐）→ 输入修订指令（如"把付款方式改为分期付款"）→ 导出 Word（版式与页面一致）
3. **演示登录**（约 30 秒）：浏览器访问 `http://localhost:58068` → 自动跳 `/login` → 输入 `.env` 中的 `DEMO_USERNAME` / `DEMO_PASSWORD` + 验证码 → 登录成功后顶部栏出现用户名与"退出登录"按钮

> 演示注意：重启后端后 session 与 token 白名单都会清空，需重新走上传流程与登录；账号密码以 `faheng-api/.env` 为准；LLM 生成耗时依赖网关负载，进度条期间可讲解规则库与三栏设计。
